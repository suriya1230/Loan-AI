version: "3.9"

services:
  backend:
    build:
      context: ../backend
    ports:
      - "5000:5000"
    command: python app.py
    restart: always

  frontend:
    build:
      context: ../frontend
    ports:
      - "3000:80"
    restart: always
